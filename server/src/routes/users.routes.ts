import { Router } from 'express'

const userRoutes = Router()

userRoutes.post('')
userRoutes.get('')
userRoutes.patch('')
userRoutes.delete('')
userRoutes.post('/login')
userRoutes.get('/user')

export default userRoutes